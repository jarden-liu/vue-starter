const PATHS = require('./paths.config');
const PORTS = require('./ports.config');
const FILE_FORMAT = require('./file-format.config');
const MODULE_ALIAS = require('./module-alias.config');
module.exports = {
  PATHS: PATHS,
  PORTS: PORTS,
  FILE_FORMAT: FILE_FORMAT,
  MODULE_ALIAS: MODULE_ALIAS,
  TITLE: 'vue-starter-demo'
};
